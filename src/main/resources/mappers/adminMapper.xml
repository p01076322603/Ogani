<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ogani.mapper.AdminMapper">

	<select id="selectById" resultType="com.ogani.domain.AdminDTO">
		SELECT * 
		FROM admin_info
		WHERE admin_id = #{admin_id}
	</select>
  
</mapper>